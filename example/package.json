{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn clean-parcel && parcel index.html",
    "build": "yarn clean-parcel && parcel build index.html",
    "clean": "yarn clean && rm -rf node_modules",
    "clean-parcel": "rm -rf .parcel-cache"
  },
  "dependencies": {
    "react-app-polyfill": "^3.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.4",
    "@types/react-dom": "^18.3.0",
    "parcel": "^2.12.0",
    "typescript": "^5.5.4"
  }
}
